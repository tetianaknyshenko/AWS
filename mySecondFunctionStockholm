import json

def lambda_handler(event, context):
    print('Hello from second function!')
    print(event)
    return {
        'statusCode': 200,
        'body': json.dumps('Hello from Lambda!')
    }
